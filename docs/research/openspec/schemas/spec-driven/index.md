# spec-driven schema

本目录是 OpenSpec 默认工作流 `spec-driven` 的 schema 定义。

## 子目录

- [templates](./templates/) — `proposal/spec/design/tasks` 四类 artifact 的模板文件。

## 文档

- [schema.yaml](./schema.yaml) — 定义 artifact 依赖顺序、每个 artifact 的生成路径/说明/指引，以及 apply 阶段的 requires/tracks/instruction。
